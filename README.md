# Основы программирования на C++

Основной репозиторий конспектов, слайдов и примеров кода курса.

## Структура

```
00-introduction.md              лекция: и страница сайта, и слайды
articles/01-wsl-setup.md        статья: только страница сайта
assets/<материал>/              картинки материала
examples/<материал>/            исходники примеров кода
```

Лекции лежат в корне репозитория и собираются в двух видах: как страница конспекта и как Reveal.js-презентация. Статьи лежат в `articles/` и собираются только как страницы сайта — это короткие практические заметки (настройка окружения, инструменты, рецепты), которые не нужно показывать со сцены.

## Локальный просмотр

1. Установите Quarto и рекомендуемое расширение `Quarto` для VS Code.
2. Откройте эту папку в VS Code.
3. Выполните:

   ```sh
   quarto preview
   ```

Собрать сайт целиком, без запуска сервера:

```sh
quarto render
```

Результат появится в `_site/`, открывать `_site/index.html`.

## Слайды

Собрать все лекции как Reveal.js-презентации:

```sh
quarto render --profile slides --to revealjs
```

Собрать только одну лекцию:

```sh
quarto render 00-introduction.md --profile slides --to revealjs
```

Результат появится в `_site/slides/`. Статьи в этот профиль не попадают.

## Новая статья

1. Создайте `articles/<номер>-<имя>.md` и укажите в заголовке `title`, `description` и авторов:

   ```yaml
   ---
   title: "Название статьи"
   description: "Одно предложение о чём это"
   author:
     - name: "Имя автора"
       url: https://t.me/nickname
   ---
   ```

2. Картинки положите в `assets/articles/<номер>-<имя>/`, примеры кода — в `examples/articles/<номер>-<имя>/`. Из статьи на них ссылаются через `../assets/...` и `../examples/...`.
3. Добавьте статью в `_quarto.yml` в раздел `Статьи` боковой панели и в список на `index.md`.

## Публикация

В настройках репозитория выберите **Settings → Pages → Build and deployment → Source: GitHub Actions**. После этого каждый push в ветку `main` будет пересобирать и публиковать конспекты и презентации.

Для pull request'ов работает отдельный workflow `Preview Quarto site`: он собирает сайт и прикладывает его к запуску как артефакт `site-preview`, чтобы изменения можно было посмотреть до мержа.
